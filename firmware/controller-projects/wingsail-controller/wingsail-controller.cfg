# ST-LINK V3 interface
source [find interface/stlink.cfg]

# Use SWD (Serial Wire Debug)
transport select hla_swd

# STM32U5 target (covers STM32U575)
source [find target/stm32u5x.cfg]

# Reset configuration
reset_config srst_only

